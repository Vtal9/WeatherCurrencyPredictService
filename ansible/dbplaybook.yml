---
- name: db deployment
  hosts: database

  tasks:
    - name: run db
      docker_container:
        name: kvv-postgres
        image: postgres
        ports:
          - "80000:5432"
        env:
          POSTGRES_PASSWORD: "postgres"