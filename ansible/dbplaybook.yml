---
- name: db deployment
  hosts: database

  tasks:
    - name: run db
      docker_conatainer:
        name: kvv-postgres
        image: postgres
        ports:
          - "5432:5432"
        env:
          POSTGRES_PASSWORD: "postgres"